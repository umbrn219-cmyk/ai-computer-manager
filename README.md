# AI Computer Manager

Initial production-quality foundation for a modular AI computer manager. This repository contains typed contracts, deterministic task transitions, a safety kernel, replaceable storage, mock platform behavior, and model routing abstractions.

## Commands

```bash
npm install
npm run typecheck
npm test
```

No network, API keys, real computer control, or external services are required by the tests.
